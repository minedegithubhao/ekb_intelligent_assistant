# resources

本目录存放后端可版本化的轻量资源。

建议用途：

- `prompts/`：LLM prompt 模板。
- `templates/`：导入、导出、邮件或文档模板。
- `seed_data/`：小型演示数据或配置种子数据。

不建议放入：

- 大模型文件。模型继续放在外部模型目录，并通过 `config/app.yaml` 引用。
- 用户上传文件。上传文件应进入对象存储、数据库记录或后续约定的数据目录。
- 构建产物和缓存。这些属于运行时数据，应进入被 Git 忽略的目录或外部服务。
